export default function RecapStep({ setState }: { setState: React.Dispatch<React.SetStateAction<any>> }) {
  return (
    <div>
      <h1 className="font-semibold text-2xl">Recap Step</h1>
    </div>
  );
}
