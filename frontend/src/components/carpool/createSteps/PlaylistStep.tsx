export default function PlaylistStep({ setState }: { setState: React.Dispatch<React.SetStateAction<any>> }) {
  return (
    <div>
      <h1 className="font-semibold text-2xl">Playlist Step</h1>
    </div>
  );
}
